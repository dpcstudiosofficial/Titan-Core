export const rarityToClass = (rarity) => `rarity-${rarity}`;

export function mechFrontSrc(id) {
  return `assets/mechs/front/${id}.svg`;
}
export function mechSideSrc(id) {
  return `assets/mechs/side/${id}.svg`;
}
